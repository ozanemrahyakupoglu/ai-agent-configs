# Rule: Git Clone / Pull

## Kurallar

- Workspace dizini: `/app/workspace`
- Repo daha önce clone'lanmadıysa `git clone` yapılır.
- Repo zaten mevcutsa `git pull` ile güncel hale getirilir.
- Ana branch'e geçilir (`main` veya `master`), sonra `git pull` yapılır.
